# запуск локального сервера с помошью Html (например index.html)
cd <имя файла> - что бы перейти в нужную папку
py -m http.server - запуск локального сервера по адресу http://localhost:8000/


# Запуск локального сервера с помошью FaspApi unicorn
# if __name__ == "__main__": - стадартный принятый способ запуска Python приложений (для закуска python main.py (или python3 или py))
# Или можно без этой абстракции = uvicorn main:app --reload (но не рукомендуеться)(нужно использовать обшепринятые нормы для ясности)
if __name__ == "__main__":
    uvicorn.run("main:app", reload=True)

